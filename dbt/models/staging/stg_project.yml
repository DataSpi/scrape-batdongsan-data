version: 2

sources:
  - name: staging
    description: "Source data from staging schema"
    tables:
      - name: real_estate
        description: "Real estate data scraped directly from batdongsan.com.vn"
models:
  - name: re_project
    description: "Extract project list from staging.real_estate"

    columns:
      - name: project_name
        description: "Name list extracted from staging.real_estate.project_name"
        tests:
          - not_null
          - unique